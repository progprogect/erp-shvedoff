# 📋 Система отмены движений остатков

**Версия документа:** 1.0  
**Дата создания:** 2025-01-XX  
**Статус:** Требования к разработке

---

## 📌 1. Контекст и бизнес-цели

### 1.1. Бизнес-проблема

В текущей системе ERP Шведoff движения остатков (stock_movements) создаются автоматически при различных операциях (производство, резка, корректировки, заказы). Однако отсутствует возможность отмены ошибочных или некорректных движений, что приводит к:

- Накоплению ошибок в остатках
- Невозможности исправить случайные ошибки операторов
- Сложности аудита и анализа истории движений
- Проблемам с целостностью данных при обнаружении ошибок

### 1.2. Бизнес-ценность

- **Операционная эффективность**: Быстрое исправление ошибок без ручных корректировок
- **Точность данных**: Поддержание целостности остатков и статистики
- **Аудит**: Прозрачная история изменений с возможностью отката
- **Удобство пользователей**: Интуитивный интерфейс для управления историей движений

### 1.3. Целевые пользователи

- **Производство** (`production`): Отмена ошибочных движений от производственных операций
- **Склад** (`warehouse`): Отмена ошибочных движений от операций резки
- **Менеджеры** (`manager`): Просмотр и отмена движений в контексте своей работы
- **Директор** (`director`): Полный доступ ко всем операциям отмены

---

## 📊 2. Текущее состояние системы

### 2.1. Архитектура движений остатков

**Таблица `stock_movements`:**
```sql
{
  id: serial PRIMARY KEY,
  productId: integer NOT NULL REFERENCES products(id),
  movementType: movement_type NOT NULL, -- 'incoming', 'outgoing', 'cutting_out', 'cutting_in', 'reservation', 'release_reservation', 'adjustment'
  quantity: integer NOT NULL,
  referenceId: integer, -- ссылка на заказ, операцию резки, производственное задание
  referenceType: varchar(50), -- 'order', 'cutting', 'production_task', 'adjustment', 'cutting_progress', 'overproduction'
  comment: text,
  userId: integer REFERENCES users(id),
  createdAt: timestamp DEFAULT NOW()
}
```

**Типы движений:**
- `incoming` — поступление товара (производство, корректировка)
- `outgoing` — списание товара
- `cutting_out` — списание исходного товара при резке
- `cutting_in` — поступление готового товара от резки
- `reservation` — резервирование под заказ
- `release_reservation` — снятие резерва
- `adjustment` — ручная корректировка остатков

### 2.2. Источники движений

**Производство (`production_task`):**
- Завершение производственного задания (`/api/production/tasks/:id/complete`)
- Частичное завершение (`/api/production/tasks/:id/partial-complete`)
- Создание товаров 2-го сорта и сорта Либерти

**Резка (`cutting`):**
- Завершение операции резки (`/api/cutting/:id/complete`)
- Добавление прогресса резки (`/api/cutting/:id/progress`)
- Создание товаров 2-го сорта и сорта Либерти

**Заказы (`order`):**
- Резервирование товаров
- Снятие резерва
- Отгрузка товаров

**Склад (`adjustment`):**
- Ручная корректировка остатков (`/api/stock/adjust`)

### 2.3. Текущие точки отображения истории

1. **Карточка товара** (`StockHistoryModal.tsx`):
   - Показывает все движения для конкретного товара
   - Статистика по типам движений
   - Кликабельные ссылки на связанные объекты

2. **Детали операции резки** (`CuttingOperations.tsx`):
   - Показывает движения, связанные с конкретной операцией резки

3. **Производственные задания** (`ProductionTasks.tsx`):
   - В настоящее время не отображает историю движений

---

## 🎯 3. Функциональные требования

### 3.1. FR-001: Отображение истории движений в разделе "Производство"

**Приоритет:** Высокий  
**Описание:** В разделе "Производство" (`/production/tasks`) должен быть добавлен отдельный таб "История движений остатков".

**Детали:**
- Таб отображается только для пользователей с правами `production:view` или выше
- Список движений фильтруется по `referenceType IN ('production_task', 'overproduction')`
- Отображаются только движения, связанные с производственными операциями
- Список сортируется по дате создания (новые сверху)

**UI Компоненты:**
- Использовать существующий компонент `Tabs` из Ant Design
- Переиспользовать логику таблицы из `StockHistoryModal.tsx`
- Адаптировать стили под существующий дизайн страницы

### 3.2. FR-002: Отображение истории движений в разделе "Резка"

**Приоритет:** Высокий  
**Описание:** В разделе "Резка" (`/cutting`) должен быть добавлен отдельный таб "История движений остатков".

**Детали:**
- Таб отображается только для пользователей с правами `cutting:view` или выше
- Список движений фильтруется по `referenceType IN ('cutting', 'cutting_progress')`
- Отображаются только движения, связанные с операциями резки
- Список сортируется по дате создания (новые сверху)

**UI Компоненты:**
- Использовать существующий компонент `Tabs` из Ant Design
- Переиспользовать логику таблицы из `StockHistoryModal.tsx`
- Адаптировать стили под существующий дизайн страницы

### 3.3. FR-003: Структура списка движений

**Приоритет:** Высокий  
**Описание:** Список движений должен содержать следующие колонки:

1. **Дата и время** (`createdAt`)
   - Формат: `DD.MM.YYYY HH:mm`
   - Сортировка по дате

2. **Количество** (`quantity`)
   - Отображение со знаком `+` или `-`
   - Единица измерения: "шт"
   - Цветовая индикация:
     - Зеленый: положительные значения (`+`)
     - Красный: отрицательные значения (`-`)

3. **Артикул товара** (`productArticle`)
   - Кликабельная ссылка
   - При клике: переход на карточку товара (`/products/:id`)
   - Отображение названия товара в tooltip

4. **Примечание** (`comment`)
   - Полный текст комментария
   - Если комментарий отсутствует, отображать "—"
   - Ellipsis для длинных текстов

5. **Действия**
   - Кнопка "Отменить" (если движение можно отменить)

**UI Компоненты:**
- Использовать `Table` из Ant Design
- Переиспользовать стили из `StockHistoryModal.tsx`
- Добавить пагинацию (10 записей на страницу)

### 3.4. FR-004: Отмена движения остатков

**Приоритет:** Критический  
**Описание:** Пользователь должен иметь возможность отменить движение остатков с подтверждением.

**Бизнес-логика:**
1. При отмене движения:
   - Откатывается изменение количества в таблице `stock`
   - Удаляется запись из `stock_movements`
   - НЕ создается новая запись в `stock_movements` (отмена не логируется)
   - Обновляется статистика операций (если применимо)

2. Валидация перед отменой:
   - Проверка, что движение не было ранее отменено
   - Проверка прав доступа пользователя

3. Ограничения на отмену:
   - Нет

**UI Компоненты:**
- Использовать `Popconfirm` из Ant Design для подтверждения
- Кнопка "Отменить" с иконкой `DeleteOutlined`
- Текст подтверждения должен содержать:
  - Информацию о движении (дата, количество, товар)
  - Предупреждение о последствиях отмены

### 3.5. FR-005: Откат изменений в остатках

**Приоритет:** Критический  
**Описание:** При отмене движения должны быть откачены изменения в остатках товара.

**Логика отката по типам движений:**

1. **`incoming`** (поступление):
   - Уменьшить `currentStock` на `quantity`
   - Формула: `currentStock = currentStock - quantity`

2. **`outgoing`** (списание):
   - Увеличить `currentStock` на `quantity`
   - Формула: `currentStock = currentStock + quantity`

3. **`cutting_out`** (списание при резке):
   - Увеличить `currentStock` на `quantity`
   - Увеличить `reservedStock` на `quantity` (если было резервирование)
   - Формула: `currentStock = currentStock + quantity`, `reservedStock = reservedStock + quantity`

4. **`cutting_in`** (поступление от резки):
   - Уменьшить `currentStock` на `quantity`
   - Формула: `currentStock = currentStock - quantity`

5. **`reservation`** (резервирование):
   - Уменьшить `reservedStock` на `quantity`
   - Формула: `reservedStock = reservedStock - quantity`

6. **`release_reservation`** (снятие резерва):
   - Увеличить `reservedStock` на `quantity`
   - Формула: `reservedStock = reservedStock + quantity`

7. **`adjustment`** (корректировка):
   - Откатить изменение: `currentStock = currentStock - quantity`
   - Формула зависит от знака `quantity`:
     - Если `quantity > 0`: `currentStock = currentStock - quantity`
     - Если `quantity < 0`: `currentStock = currentStock + abs(quantity)`

**Технические требования:**
- Все операции должны выполняться в транзакции
- Проверка целостности данных после отката
- Валидация отрицательных остатков (разрешить, если система поддерживает отрицательные остатки)

### 3.6. FR-006: Удаление записи из истории

**Приоритет:** Высокий  
**Описание:** При отмене движения запись должна быть удалена из таблицы `stock_movements`, а НЕ помечена как отмененная.

**Обоснование:**
- Отмененные движения не должны отображаться в истории товара
- Упрощение логики фильтрации
- Соответствие требованию: "если указан лог, то он просто удаляется"

**Технические требования:**
- Использовать `DELETE FROM stock_movements WHERE id = ?`
- Выполнять в той же транзакции, что и откат остатков
- НЕ создавать запись в `audit_log` для отмены движения (это не изменение бизнес-объекта)

### 3.7. FR-007: Обновление статистики операций

**Приоритет:** Средний  
**Описание:** При отмене движения, связанного с операцией резки или производством, должна быть обновлена статистика операции.

**Детали:**
1. **Для операций резки:**
   - Если отменяется `cutting_in` (готовый товар):
     - Обновить `cutting_progress_log` (если есть)
     - Пересчитать прогресс операции резки
   - Если отменяется `cutting_out` (исходный товар):
     - Обновить прогресс операции резки

2. **Для производственных заданий:**
   - Если отменяется `incoming` (производство):
     - Обновить `producedQuantity` в `production_tasks`
     - Пересчитать статус задания

**Технические требования:**
- Обновление статистики должно быть атомарным (в транзакции)
- Проверка целостности данных после обновления

### 3.8. FR-008: Права доступа

**Приоритет:** Высокий  
**Описание:** Доступ к отмене движений должен контролироваться системой разрешений.

**Права доступа:**

1. **Просмотр истории движений:**
   - `production:view` — для раздела "Производство"
   - `cutting:view` — для раздела "Резка"
   - `stock:view` — для общего просмотра (в карточке товара)

2. **Отмена движений:**
   - `production:manage` — для отмены движений от производства
   - `cutting:manage` — для отмены движений от резки
   - `stock:manage` — для отмены движений от корректировок

**Проверка прав:**
- Backend: проверка через `requirePermission` middleware
- Frontend: проверка через `usePermissions` hook
- Отображение кнопки "Отменить" только при наличии прав

---

## 🔄 4. Трассировка требований

### 4.1. Зависимости между требованиями

```
FR-001 (История в Производстве)
    ├── FR-003 (Структура списка)
    ├── FR-004 (Отмена движения)
    ├── FR-005 (Откат остатков)
    ├── FR-006 (Удаление записи)
    ├── FR-007 (Обновление статистики)
    └── FR-008 (Права доступа)

FR-002 (История в Резке)
    ├── FR-003 (Структура списка)
    ├── FR-004 (Отмена движения)
    ├── FR-005 (Откат остатков)
    ├── FR-006 (Удаление записи)
    ├── FR-007 (Обновление статистики)
    └── FR-008 (Права доступа)
```

### 4.2. Влияние на существующий функционал

**Затронутые модули:**

1. **Backend:**
   - `backend/src/routes/stock.ts` — новый endpoint для отмены движений
   - `backend/src/routes/production.ts` — чтение движений для фильтрации
   - `backend/src/routes/cutting.ts` — чтение движений для фильтрации
   - `backend/src/utils/stockManager.ts` — логика отката остатков
   - `backend/src/db/schema.ts` — без изменений (используем существующую схему)

2. **Frontend:**
   - `frontend/src/pages/ProductionTasks.tsx` — добавление таба "История движений"
   - `frontend/src/pages/CuttingOperations.tsx` — добавление таба "История движений"
   - `frontend/src/services/stockApi.ts` — новый метод для отмены движений
   - `frontend/src/components/StockHistoryModal.tsx` — без изменений (переиспользование логики)

3. **База данных:**
   - Без изменений схемы (используем существующую таблицу `stock_movements`)

### 4.3. Риски и зависимости

**Технические риски:**

1. **Race conditions при отмене:**
   - Риск: Одновременная отмена нескольких движений может привести к конфликтам
   - Митигация: Использование транзакций с уровнем изоляции `SERIALIZABLE` или `REPEATABLE READ`

2. **Целостность данных:**
   - Риск: Отмена движения может привести к отрицательным остаткам
   - Митигация: Валидация перед отменой, предупреждение пользователя

3. **Каскадные эффекты:**
   - Риск: Отмена движения может нарушить связанные операции (заказы, резервы)
   - Митигация: Проверка связей перед отменой, блокировка отмены для активных операций

4. **Производительность:**
   - Риск: Большой объем движений может замедлить загрузку списка
   - Митигация: Пагинация, индексы на `referenceType` и `createdAt`

**Бизнес-риски:**

1. **Злоупотребление функционалом:**
   - Риск: Пользователи могут отменять движения без необходимости
   - Митигация: ограничение прав доступа

2. **Потеря аудита:**
   - Риск: Удаление движений затрудняет аудит
   - Митигация: Логирование отмен в `audit_log` (отдельная запись)

---

## 👤 5. Пользовательские сценарии

### 5.1. UC-001: Отмена ошибочного движения от производства

**Актор:** Пользователь с ролью `production`

**Предусловия:**
- Пользователь авторизован
- Пользователь имеет права `production:view` и `production:manage`
- Существует производственное задание с завершенным движением остатков

**Основной поток:**
1. Пользователь открывает раздел "Производство" (`/production/tasks`)
2. Пользователь переходит на таб "История движений остатков"
3. Система отображает список движений, связанных с производством
4. Пользователь находит движение с ошибкой (например, неверное количество)
5. Пользователь нажимает кнопку "Отменить" рядом с движением
6. Система показывает модальное окно подтверждения с деталями движения
7. Пользователь подтверждает отмену
8. Система выполняет отмену:
   - Откатывает изменение остатков
   - Удаляет запись из истории
   - Обновляет статистику производственного задания
9. Система показывает уведомление об успешной отмене
10. Список движений обновляется (движение исчезает)

**Альтернативный поток 5a: Отмена невозможна** (хотя таких ограничений сейчас нет)
- Пользователь видит сообщение: "Невозможно отменить движение, ошибка: и инфор мация об ошибке"

**Альтернативный поток 7a: Пользователь отменяет подтверждение**
- Пользователь закрывает модальное окно без подтверждения
- Отмена не выполняется

**Постусловия:**
- Остатки товара обновлены
- Движение удалено из истории
- Статистика производственного задания обновлена

### 5.2. UC-002: Отмена ошибочного движения от резки

**Актор:** Пользователь с ролью `production` или `warehouse`

**Предусловия:**
- Пользователь авторизован
- Пользователь имеет права `cutting:view` и `cutting:manage`
- Существует операция резки с завершенным движением остатков

**Основной поток:**
1. Пользователь открывает раздел "Резка" (`/cutting`)
2. Пользователь переходит на таб "История движений остатков"
3. Система отображает список движений, связанных с резкой
4. Пользователь находит движение с ошибкой
5. Пользователь нажимает кнопку "Отменить" рядом с движением
6. Система показывает модальное окно подтверждения
7. Пользователь подтверждает отмену
8. Система выполняет отмену:
   - Откатывает изменение остатков
   - Удаляет запись из истории
   - Обновляет прогресс операции резки
9. Система показывает уведомление об успешной отмене
10. Список движений обновляется

**Альтернативный поток 5a: Отмена невозможна** (хотя таких ограничений сейчас нет)
- Пользователь видит сообщение: "Невозможно отменить движение, ошибка: и инфор мация об ошибке"

**Постусловия:**
- Остатки товара обновлены
- Движение удалено из истории
- Прогресс операции резки обновлен

### 5.3. UC-003: Просмотр истории движений без прав на отмену

**Актор:** Пользователь с ролью `warehouse` (только просмотр)

**Предусловия:**
- Пользователь авторизован
- Пользователь имеет права `cutting:view`, но НЕ имеет `cutting:manage`

**Основной поток:**
1. Пользователь открывает раздел "Резка" (`/cutting`)
2. Пользователь переходит на таб "История движений остатков"
3. Система отображает список движений
4. Пользователь видит все движения, но кнопка "Отменить" не отображается
5. Пользователь может кликнуть на артикул товара для перехода на карточку товара

**Постусловия:**
- Пользователь видит историю движений
- Пользователь не может отменить движения

---

## ✅ 6. Acceptance Criteria (AC)

### AC-001: Отображение истории в разделе "Производство"

**Given:** Пользователь с правами `production:view` открывает раздел "Производство"  
**When:** Пользователь переходит на таб "История движений остатков"  
**Then:**
- ✅ Отображается список движений с `referenceType IN ('production_task', 'overproduction')`
- ✅ Список отсортирован по дате создания (новые сверху)
- ✅ Отображаются колонки: Дата, Количество, Артикул, Примечание, Действия
- ✅ Артикул товара кликабелен и ведет на карточку товара
- ✅ Кнопка "Отменить" отображается только для пользователей с правами `production:manage`

### AC-002: Отображение истории в разделе "Резка"

**Given:** Пользователь с правами `cutting:view` открывает раздел "Резка"  
**When:** Пользователь переходит на таб "История движений остатков"  
**Then:**
- ✅ Отображается список движений с `referenceType IN ('cutting', 'cutting_progress')`
- ✅ Список отсортирован по дате создания (новые сверху)
- ✅ Отображаются колонки: Дата, Количество, Артикул, Примечание, Действия
- ✅ Артикул товара кликабелен и ведет на карточку товара
- ✅ Кнопка "Отменить" отображается только для пользователей с правами `cutting:manage`

### AC-003: Отмена движения с подтверждением

**Given:** Пользователь с правами на отмену видит движение в списке  
**When:** Пользователь нажимает кнопку "Отменить"  
**Then:**
- ✅ Отображается модальное окно подтверждения (`Popconfirm`)
- ✅ В модальном окне показаны детали движения (дата, количество, товар)
- ✅ Предупреждение о последствиях отмены
- ✅ Кнопки "Подтвердить" и "Отмена"

### AC-004: Откат остатков при отмене

**Given:** Пользователь подтверждает отмену движения типа `incoming` с количеством `+10`  
**When:** Система выполняет отмену  
**Then:**
- ✅ `currentStock` уменьшается на `10` (откат)
- ✅ Изменения выполняются в транзакции
- ✅ После отмены остатки соответствуют ожидаемым значениям

### AC-005: Удаление записи из истории

**Given:** Пользователь подтверждает отмену движения  
**When:** Система выполняет отмену  
**Then:**
- ✅ Запись удаляется из таблицы `stock_movements`
- ✅ Движение больше не отображается в списке истории
- ✅ Движение не отображается в карточке товара (`StockHistoryModal`)

### AC-006: Валидация перед отменой - сейчас можно пропустить.

**Given:** Пользователь пытается отменить движение
**When:** Система проверяет возможность отмены  
**Then:**
- ✅ Система блокирует отмену
- ✅ Показывается сообщение об ошибке: "Невозможно отменить движение, ошибка: "
- ✅ Остатки не изменяются

### AC-007: Обновление статистики операций

**Given:** Пользователь отменяет движение `cutting_in` от операции резки  
**When:** Система выполняет отмену  
**Then:**
- ✅ Прогресс операции резки пересчитывается
- ✅ Статистика в деталях операции резки обновляется
- ✅ Изменения выполняются атомарно (в транзакции)

### AC-008: Права доступа

**Given:** Пользователь с ролью `warehouse` (только `cutting:view`) открывает раздел "Резка"  
**When:** Пользователь переходит на таб "История движений остатков"  
**Then:**
- ✅ Список движений отображается
- ✅ Кнопка "Отменить" НЕ отображается
- ✅ При попытке вызвать API отмены напрямую возвращается ошибка 403

---

## 🏗️ 7. Техническая архитектура

### 7.1. Backend API

**Новый endpoint:**

```typescript
DELETE /api/stock/movements/:movementId
```

**Параметры:**
- `movementId` (path) — ID движения для отмены

**Ответ:**
```json
{
  "success": true,
  "message": "Движение успешно отменено",
  "data": {
    "movementId": 123,
    "productId": 45,
    "newStock": 100,
    "newReservedStock": 10
  }
}
```

**Ошибки:**
- `400` — Невозможно отменить движение (связано с активной операцией)
- `403` — Недостаточно прав
- `404` — Движение не найдено
- `500` — Ошибка сервера

**Middleware:**
- `authenticateToken` — проверка авторизации
- `requirePermission('stock', 'manage')` или специфичные права в зависимости от типа движения

### 7.2. Frontend API Client

**Новый метод в `stockApi.ts`:**

```typescript
async cancelMovement(movementId: number): Promise<ApiResponse<CancelMovementResponse>> {
  const response = await axios.delete(
    `${API_BASE_URL}/stock/movements/${movementId}`,
    this.getAuthHeaders()
  );
  return response.data;
}
```

### 7.3. Компоненты Frontend

**Новый компонент (или переиспользование):**

```typescript
// frontend/src/components/StockMovementsList.tsx
interface StockMovementsListProps {
  referenceTypes: string[];
  onCancel?: (movementId: number) => void;
  canCancel?: boolean;
}
```

**Интеграция в существующие страницы:**

1. **ProductionTasks.tsx:**
   - Добавить таб "История движений" в `Tabs`
   - Использовать `StockMovementsList` с `referenceTypes={['production_task', 'overproduction']}`
   - Проверка прав: `canCancel={canManage('production')}`

2. **CuttingOperations.tsx:**
   - Добавить таб "История движений" в `Tabs`
   - Использовать `StockMovementsList` с `referenceTypes={['cutting', 'cutting_progress']}`
   - Проверка прав: `canCancel={canManage('cutting')}`

### 7.4. База данных

**Без изменений схемы:**
- Используем существующую таблицу `stock_movements`
- Используем существующую таблицу `stock`

**Рекомендуемые индексы (если отсутствуют):**
```sql
CREATE INDEX IF NOT EXISTS idx_stock_movements_reference_type 
ON stock_movements(reference_type, created_at DESC);

CREATE INDEX IF NOT EXISTS idx_stock_movements_product_id 
ON stock_movements(product_id, created_at DESC);
```

---

## 📐 8. UX/UI Требования

### 8.1. Дизайн списка движений

- **Таблица:** Использовать `Table` из Ant Design
- **Стили:** Переиспользовать стили из `StockHistoryModal.tsx`
- **Пагинация:** 10 записей на страницу
- **Сортировка:** По дате создания (DESC)

### 8.2. Кнопка отмены

- **Иконка:** `DeleteOutlined` из Ant Design
- **Цвет:** Красный (`danger`)
- **Размер:** `small`
- **Текст:** "Отменить"
- **Tooltip:** "Отменить это движение остатков"

### 8.3. Модальное окно подтверждения

- **Компонент:** `Popconfirm` из Ant Design
- **Заголовок:** "Подтвердите отмену движения"
- **Текст:** Детали движения (дата, количество, товар, примечание)
- **Предупреждение:** "Это действие откатит изменения в остатках и удалит запись из истории"
- **Кнопки:** "Подтвердить" (красная) и "Отмена"

### 8.4. Уведомления

- **Успех:** `message.success('Движение успешно отменено')`
- **Ошибка:** `message.error('Невозможно отменить движение: [причина]')`
- **Загрузка:** `Spin` на кнопке отмены во время выполнения

### 8.5. Консистентность

- Использовать существующие компоненты Ant Design
- Следовать стилям из `StockHistoryModal.tsx`
- Сохранять единый стиль с остальными разделами системы

---

## 🔒 9. Безопасность и валидация

### 9.1. Валидация на Backend

1. **Проверка существования движения:**
   ```typescript
   const movement = await db.query.stockMovements.findFirst({
     where: eq(schema.stockMovements.id, movementId)
   });
   if (!movement) throw new Error('Движение не найдено');
   ```

2. **Проверка прав доступа:**
   - В зависимости от `referenceType` проверять соответствующие права
   - `production_task` → `production:manage`
   - `cutting` → `cutting:manage`
   - `adjustment` → `stock:manage`

3. **Проверка связей:**
   - Если `referenceType === 'order'`
   - Если `referenceType === 'cutting'`
   - Если `referenceType === 'production_task'`

4. **Проверка целостности данных:**
   - После отката остатков проверять, что `currentStock >= 0` (или разрешить отрицательные, если система поддерживает) - мы решили, что будем разрешать.

### 9.2. Логирование

- Логировать все попытки отмены (успешные и неуспешные)
- Сохранять в `audit_log`:
  - `operation: 'CANCEL_MOVEMENT'`
  - `tableName: 'stock_movements'`
  - `recordId: movementId`
  - `userId: currentUser.id`
  - `newValues: { cancelled: true, originalMovement: movement }`

---

## 📊 10. Метрики успеха

### 10.1. Функциональные метрики

- ✅ Пользователи могут отменить ошибочные движения
- ✅ Остатки корректно откатываются после отмены
- ✅ История движений не содержит отмененных записей
- ✅ Статистика операций обновляется корректно

### 10.2. Производительность

- Загрузка списка движений: < 1 секунда (для 100 записей)
- Отмена движения: < 500 мс

### 10.3. UX метрики

- Пользователи понимают, как отменить движение (интуитивный интерфейс)
- Количество ошибок отмены: < 5% от всех попыток

---

## 📝 11. Дополнительные замечания

### 11.1. Ограничения

- Нельзя отменять движения старше 30 дней (опционально, для безопасности).

### 11.2. Будущие улучшения - сейчас не реализуем

- Добавить фильтрацию по дате, товару, типу движения
- Добавить экспорт истории движений
- Добавить массовую отмену движений (для администраторов)
- Добавить причину отмены (комментарий при отмене)

---

## 📚 12. Ссылки на связанные документы

- `WBS.txt` — структура проекта
- `Функциональные требования.txt` — общие функциональные требования
- `STOCK-MANAGEMENT.md` — документация по управлению остатками
- `PERMISSIONS_AUDIT.md` — документация по системе разрешений

---

**Автор документации:** AI Assistant  
**Дата последнего обновления:** 2025-01-XX  
**Статус:** Готово к реализации

